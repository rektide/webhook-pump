{
	"name": "webpush-pump",
	"version": "1.0.0",
	"description": "Create an event listener and a Koa handler, piping events from the listener out as HTTP2 Push resources to HTTP2 clients who have touched the Koa handler endpoint.",
	"main": "webhook-pump.js",
	"scripts": {
		"test": "node .test",
		"files": "find . -path ./node_modules -prune -o -path ./\\.git -prune -o -name '.*' -o -type f -print"
	},
	"repository": {
		"type": "git",
		"url": "https://archive.voodoowarez.com/webhook-pump"
	},
	"author": "rektide de la faye",
	"license": "X",
	"dependencies": {
		"generator-compose": "^0.1.0",
		"insure-classiness": "^1.1.0",
		"node-uuid": "^1.4.3",
		"observed-time-projection": "^1.0.1"
	},
	"peerDependencies": {
		"koa": "^1.0.0",
		"koa-router": "^5.2.3",
		"node-spdy": "^2.0.5"
	},
	"devDependencies": {
		"koa-router": "^5.2.3",
		"selfsigned": "^1.2.0",
		"tape": "^4.2.1"
	}
}
